# Phase 03: README And Deploy

## Objectives

1. Update the README to describe the scrollytelling assignment result.
2. Add the GitHub Pages URL prominently.
3. Verify the production build before deployment.

## Exit checks

- `README.md` includes the live Pages URL.
- `npm run build` succeeds.
- Deployment command is ready or completed.

## Completion notes

- Rewrote the README to foreground the assignment deliverable and live URLs.
- Verified the production build with `npm run build`.
- Published the updated `dist/` folder with `npm run deploy`.
