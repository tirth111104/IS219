# Phase 01: Story Foundation

## Objectives

1. Create durable reference/spec/phase files inside the project.
2. Reframe the homepage around a scrollytelling narrative.
3. Keep the interactive affordability assistant available as part of the page.

## Constraints

- Stay within the current Vite + React stack.
- Reuse existing project ideas instead of inventing a brand-new topic.
- Do not require login before the instructor can see the work.

## Exit checks

- `npm run build` succeeds.
- The homepage is a scrollytelling page instead of a simple app shell.

## Completion notes

- Added a teacher context pack plus stable specs and phase files.
- Replaced the login-first app shell with a scrollytelling homepage.
- Kept the live tool visible by embedding the chart, calculator, and chatbot lower on the page.
